#!/usr/bin/env bash
set -euo pipefail

root="$(git rev-parse --show-toplevel)"
cd "$root"

# Staged paths only. Full-tree `dotenvx precommit` walks .worktrees and takes ~10s+.
git diff --cached --name-only --diff-filter=ACM | ./scripts/githooks/check-staged-env.sh
